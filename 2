function change_var(para1, para2){
    if(para1 && para2){
        document.write("paras are: "+para1+" and "+para2);
    }
    else{
	window.alert("Missing Parameter!");
    }
   
}
change_var(10, 20);
